@model ZoipSwitch.Models.action_categories

@{
    Layout = null;
}

<div>
    <table class="table table-striped details">
        <tr>
            <td class="col-xs-4 border-box" style="font-weight:bold; text-align:right">@Html.DisplayNameFor(model => model.action_category_name)</td>
            <td class="col-xs-8 border-box">@Html.DisplayFor(model => model.action_category_name)</td>
        </tr>
    </table>
</div>

